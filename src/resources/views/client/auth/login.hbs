<div class="site-section py-5">
    <div class="site-section">
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h3 class="h3 mb-5 text-black"> Login</h3>
                </div>
                <div class="col-md-12">
                    <form method="POST" action="/auth/login">
                        <div class="p-3 p-lg-5 border">
                            <div class="form-group row">
                                <div class="col-md-12">
                                    <label for="c_email" class="text-black">Email <span
                                            class="text-danger">*</span></label>
                                    <input type="email" class="form-control" id="email" name="email" placeholder=""
                                        value="{{#if inputData.email }}{{inputData.email}}{{/if}}">
                                    <p style=" color: red;">
                                        {{#if errors.email }}
                                        {{errors.email.msg}}
                                        {{/if}}
                                    </p>
                                </div>
                            </div>

                            <div class="form-group row">
                                <div class="col-md-12">
                                    <label for="password" class="text-black">Password <span
                                            class="text-danger">*</span></label>
                                    <input type="password" class="form-control" id="password" name="password"
                                        placeholder="" value="{{#if inputData.password }}{{inputData.password}}{{/if}}">
                                    <p style=" color: red;">
                                        {{#if errors.password }}
                                        {{errors.password.msg}}
                                        {{/if}}
                                    </p>
                                </div>
                            </div>
                            <button type="submit" class="btn btn-primary btn-lg btn-block">Login</button>
                        </div>
                    </form>
                </div>

            </div>
        </div>
    </div>

    {{!-- <h3>Login</h3>

    <form method="POST" action="/auth/login">
        <div class="form-group">
            <label for="email">Email</label>
            <input type="text" class="form-control" id="email" name="email"
                value="{{#if inputData.email }}{{inputData.email}}{{/if}}">
            <p style=" color: red;">
                {{#if errors.email }}
                {{errors.email.msg}}
                {{/if}}
            </p>
        </div>
        <div class="form-group">
            <label for="password">Password</label>
            <input type="password" class="form-control" id="password" name="password"
                value="{{#if inputData.password }}{{inputData.password}}{{/if}}">
            <p style="color: red;">
                {{#if errors.password }}
                {{errors.password.msg}}
                {{/if}}
            </p>
        </div>

        <button type="submit" class="btn btn-primary">Login</button>
    </form>
</div> --}}
{{#if sessionFlash}}
<script>
    // Set the options that I want
    toastr.error('{{sessionFlash.message}}');
</script>
{{/if}}