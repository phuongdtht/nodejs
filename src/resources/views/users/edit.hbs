<div class="mt-4">
    <h3>Cập nhật nguoi dung</h3>

    <form class="mt-4" method="POST" action="/users/{{user._id}}?_method=PUT">
        <div class="form-group">
            <label for="name">Tên nguoi dung</label>
            <input type="text" class="form-control" value="{{user.name}}" id="name" name="name">
        </div>
        <div class="form-group">
            <label for="email">Email</label>
            <input class="form-control" id="description" value="{{user.email}}" name="email">
        </div>
        {{!-- <div class="form-group">
            <label for="videoId">Video ID</label>
            <input type="text" class="form-control" value="{{course.videoId}}" id="videoId" name="videoId">
        </div> --}}
        {{!-- <div class="form-group">
            <label for="level">Trình độ</label>
            <input type="text" class="form-control" value="{{course.level}}" id="level" name="level">
        </div> --}}

        <button type="submit" class="btn btn-primary">Lưu lại</button>
    </form>
</div>